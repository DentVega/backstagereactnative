/** Module Federation remote — resolved at runtime (URL from Backstage). */
declare module 'account_dashboard/Entry' {
  import type {ComponentType} from 'react';
  import type {MiniappEntryProps} from '@org/miniapp-contract';
  const Entry: ComponentType<MiniappEntryProps>;
  export default Entry;
}
