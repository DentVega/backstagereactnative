# S1.2 — Middleware que gatea la UI
**Como** plataforma, **quiero** proteger la UI, **para** exigir login.
## Aceptación
- Protege /, /catalog, /miniapp/*, /create → sin sesión → redirect /signin.
- Excluye /api/resolve, /upload, /scaffold, /seed.
