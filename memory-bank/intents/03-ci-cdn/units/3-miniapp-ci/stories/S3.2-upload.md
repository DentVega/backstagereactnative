# S3.2 — Empaquetar + subir a Backstage
**Como** CI, **quiero** subir el build, **para** publicar automáticamente.
## Aceptación
- zip de build/ → POST /api/miniapps/<id>/upload (Bearer PUBLISH_TOKEN, version, manifest).
- Dispara en push main / tag v*.
