# S2.2 — POST /api/miniapps/:id/upload
**Como** CI, **quiero** subir el build, **para** publicar la miniapp.
## Aceptación
- Autenticado (Bearer PUBLISH_TOKEN); zip del build + version + manifest.
- Descomprime → Blob → publishVersion(url=CDN). Errores 401/403/400/404/409/502.
