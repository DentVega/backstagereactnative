# S2.1 — ChunkStorage (interfaz + Vercel Blob)
**Como** Backstage, **quiero** almacenar chunks en Blob, **para** servirlos por CDN.
## Aceptación
- Interfaz `ChunkStorage.putMany(prefix, files) → {baseUrl}` (Blob público) + mock.
- Impl Vercel Blob sube bajo `<id>/<version>/`.
