# S2.1 — Navegación base + shell de auth/sesión (mock)

**Como** usuario, **quiero** navegación y una sesión activa, **para** acceder a las secciones de la app; **como** host, mantengo la sesión y expongo capabilities scoped a las miniapps.

## Aceptación
- native-stack con al menos Home → Dashboard.
- Store de sesión (Zustand) mock; sesión vive **solo en host**.
- Función `getScopedCapabilities()` que produce capabilities **revocables**, nunca credenciales.
- ≥1 test RNTL de navegación/estado de sesión.

## Notas
- Host · sin nativos · seguridad: nada de credenciales crudas hacia remotes.
