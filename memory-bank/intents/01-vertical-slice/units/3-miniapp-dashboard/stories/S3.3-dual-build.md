# S3.3 — Build dual (remote MF + shared package)

**Como** plataforma, **quiero** que la miniapp se construya como remote y como paquete compartido, **para** consumirla en runtime o en build-time desde el mismo código.

## Aceptación
- Config MF que produce un **container remoto** cargable (chunk).
- Empaquetado como **shared package** `@org/account-dashboard` importable en build-time.
- Lista `shared` = subconjunto compatible de los singletons del host (verificado).
- Documentar ambos entrypoints y mantenerlos en sync.

## Notas
- Remote federado · sin nativos · valida el consumo dual del intent (US-5).
