# S3.2 — Pantalla Dashboard + entry ./Entry

**Como** usuario, **quiero** ver mis saldos y últimos movimientos, **para** revisar mi cuenta de un vistazo.

## Aceptación
- Header de saldo + **FlashList** de movimientos, usando `ui-kit` (tokens, sin hex crudos).
- Datos **mock** vía React Query (cache singleton compartida).
- `expose: "./Entry"` recibe `capabilities` (contrato) — sin credenciales crudas.
- 60 FPS en scroll; ≥1 **test RNTL** de la pantalla (regla dura: bolt de pantalla sin test no está done).

## Notas
- Remote federado · solo-JS · sin nativos.
