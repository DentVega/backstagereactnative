# S1.2 — Scaffold host RN + Re.Pack (MF v2)

**Como** dev, **quiero** el host RN configurado con Re.Pack (Rspack + Module Federation v2), **para** tener la base que montará remotes — **sin Metro**.

## Aceptación
- `apps/host` arranca en emulador con pantalla vacía servida por **Re.Pack** (no Metro).
- Container MF `host` declarado; lista `shared` singleton (react, react-native, navigation, zustand, @tanstack/react-query).
- `tsc` verde.

## Notas
- Host · sin nativos · usar `/repack-init` como apoyo de scaffold.
