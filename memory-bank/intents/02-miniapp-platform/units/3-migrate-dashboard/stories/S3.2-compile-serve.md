# S3.2 — Compila + dev server + tests

**Como** equipo, **quiero** compilar y servir el chunk, **para** que el host lo consuma.

## Aceptación
- Compila el remote fuera del monorepo; dev server sirve el chunk (puerto dedicado).
- Los 13 tests de la miniapp pasan en el nuevo repo.
