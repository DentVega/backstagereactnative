# S3.1 — Crear miniapp-account-dashboard (mover código)

**Como** equipo, **quiero** account-dashboard en su repo, **para** desarrollarla independiente.

## Aceptación
- Nuevo repo con el código de `miniapps/account-dashboard` (dominio, pantalla, Entry, tests).
- Deps a `@org/*` **publicados** (no workspace); `git init`.
