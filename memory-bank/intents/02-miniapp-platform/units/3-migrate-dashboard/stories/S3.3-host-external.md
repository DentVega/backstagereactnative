# S3.3 — Host consume la miniapp externa

**Como** host, **quiero** resolver/montar la miniapp externa, **para** desacoplarla del monorepo.

## Aceptación
- Quitar `miniapps/*` del `pnpm-workspace.yaml`; host verde (tsc + tests).
- Backstage seed/registro apunta a la URL externa; host resuelve coherente (montaje real depende del entorno nativo).
