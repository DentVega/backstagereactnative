# S2.3 — Placeholders + README + marcar como template

**Como** scaffolder, **quiero** placeholders y un README, **para** generar miniapps consistentes.

## Aceptación
- Placeholders `__MINIAPP_ID__` / `__MINIAPP_NAME__` / `__MINIAPP_OWNER__` presentes; mecanismo de sustitución por ADR (Design).
- README con runbook de dev (dev server, puerto, publicar URL a Backstage).
- Repo marcado como **template** en GitHub.
