# S2.2 — Deps publicados + compila standalone

**Como** miniapp dev, **quiero** que el template use los `@org/*` publicados, **para** no depender del monorepo.

## Aceptación
- `package.json` depende de `@org/miniapp-contract` + `@org/ui-kit` (rango) desde GitHub Packages; `.npmrc` incluido.
- El template **compila** un chunk remote (`webpack-bundle`) que expone `./Entry`.
