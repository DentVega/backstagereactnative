# S4.2 — POST /api/scaffold

**Como** platform dev, **quiero** un endpoint que cree + registre la miniapp.

## Aceptación
- Valida `id` (parseMiniappId), no-existe → `createFromTemplate` → `registerMiniapp` → `{repoUrl}`.
- Errores: 400 id inválido, 409 ya existe, 502 fallo git.
- Tests de API (con GitProvider mockeado).
