# S4.1 — GitProvider (interfaz + impl GitHub)

**Como** Backstage, **quiero** crear repos desde el template, **para** scaffoldear miniapps.

## Aceptación
- Interfaz `GitProvider.createFromTemplate({templateRepo,name,owner})`.
- Impl `githubProvider` (API REST `generate from template`, token env); mock inyectable.
- Unit tests con el provider mockeado.
