# S4.3 — Página /create (mínima)

**Como** platform dev, **quiero** un formulario, **para** crear miniapps sin curl.

## Aceptación
- `/create`: form (id, name, owner) → llama al endpoint → muestra el `repoUrl`.
- Test de componente + integración con la API.
