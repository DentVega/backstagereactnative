# S1.2 — Verificar publicación de contract + ui-kit

**Como** plataforma, **quiero** publicar ambos paquetes a GitHub Packages, **para** que se instalen por versión.

## Aceptación
- `npm pack` limpio de ambos (tarball con `dist` + `.d.ts`).
- (Opcional, con token) `npm publish` real a GitHub Packages; o dry-run verificado.
- Versión semver coherente; changelog si el contrato cambia.

## Notas
- Token `write:packages` por env/CI; nunca en código.
