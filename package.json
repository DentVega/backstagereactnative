{
  "name": "backstagereactnative-monorepo",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build:packages": "pnpm -r --filter \"./packages/**\" --if-present build"
  },
  "devDependencies": {
    "@babel/plugin-syntax-typescript": "^7.25.2",
    "@react-native/community-cli-plugin": "0.76.6",
    "@swc/helpers": "^0.5.23"
  }
}
